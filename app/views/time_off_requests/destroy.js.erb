$('.modal').modal('hide')
$('.modal-backdrop').remove();
$('body').removeClass('modal-open');
$('table tr td').find('a[href="/time_off_requests/<%= @time_off_request.id %>"]').parent().parent().remove();
$('.flash-wrapper').html("<%= escape_javascript(render 'layouts/flash_messages') %>");
