if ($("#<%=@item.id%>").length){
  $("#<%=@item.id%>").val( function(i, oldval) {
    return ++oldval;
  });
  $("#item_id").val("").focus();
  $("#checkin").removeClass("hidden");
}
else {
  $("#item_id").val("").focus();
  $("#checkin").removeClass("hidden");
  $("#checkin-items").append("<%= escape_javascript(render 'item')%>");
}
