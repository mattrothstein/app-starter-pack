<%= form_for(@inventory_item_unit) do |f| %>
  <% if @inventory_item_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inventory_item_unit.errors.count, "error") %> prohibited this unit from being saved:</h2>

      <ul>
      <% @inventory_item_unit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :references %>
    <%= f.text_field :references %>
  </div>

  <div class="field">
    <%= f.label :checked_out %>
    <%= f.date_select :checked_out %>
  </div>

  <div class="field">
    <%= f.label :checked_in %>
    <%= f.date_select :checked_in %>
  </div>

  <div class="field">
    <%= f.label :checked_out_by %>
    <%= f.text_field :checked_out_by %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
