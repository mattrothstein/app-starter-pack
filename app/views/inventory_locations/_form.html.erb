<%= form_for @inventory_location, :html => { :class => "form-horizontal inventory_location" } do |f| %>

  <% if @inventory_location.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@inventory_location.errors.count, "error") %> prohibited this inventory_location from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @inventory_location.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%=f.error_span(:name) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                inventory_locations_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
